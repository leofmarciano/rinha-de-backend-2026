services:
  lb:
    image: haproxy:3.3
    cpus: "0.20"
    mem_limit: "50MB"
    ports:
      - "9999:9999"
    volumes:
      - ./deploy/haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
      - sockets:/sockets
    depends_on:
      - api1
      - api2
    deploy:
      resources:
        limits:
          cpus: "0.20"
          memory: "50MB"

  api1:
    image: ghcr.io/leofmarciano/rinha-de-backend-2026:454340dde3baf804df375dbc3e0b290fc73a7cc9
    platform: linux/amd64
    security_opt:
      - seccomp=unconfined
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
    cpus: "0.40"
    mem_limit: "150MB"
    environment: &api-env
      PORT: "8080"
      SOCKET_PATH: "/sockets/api1.sock"
      INDEX_PATH: "/app/build/index_k8192.ivfi16"
      BASE_NPROBE: "16"
      AMBIG_NPROBE: "32"
      BBOX_MODE: "ambiguous-only"
      USE_EXACT_FALLBACK: "0"
      USE_FAST_PATH: "0"
      USE_IOURING: "1"
      HEURISTIC_ONLY: "0"
      WARMUP: "full"
      WORKERS: "1"
    volumes:
      - sockets:/sockets
    deploy:
      resources:
        limits:
          cpus: "0.40"
          memory: "150MB"

  api2:
    image: ghcr.io/leofmarciano/rinha-de-backend-2026:454340dde3baf804df375dbc3e0b290fc73a7cc9
    platform: linux/amd64
    security_opt:
      - seccomp=unconfined
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
    cpus: "0.40"
    mem_limit: "150MB"
    environment:
      <<: *api-env
      SOCKET_PATH: "/sockets/api2.sock"
    volumes:
      - sockets:/sockets
    deploy:
      resources:
        limits:
          cpus: "0.40"
          memory: "150MB"

volumes:
  sockets:
